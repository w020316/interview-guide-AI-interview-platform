# Cloudflare Pages 响应头配置
#
# ⚠️ 格式约束（2026-09-21 线上实测踩坑）：
#   **注释行绝不能出现在某个路径区块内部**（即在「路径行」与「该区块的 header 行」之间），
#   否则 Cloudflare 会整块忽略——不报错、不告警，静默失效。
#   实测：原 `/*` 区块把 `# 安全响应头（全站）` 写在 `/*` 之后，结果该区块的
#   X-Frame-Options / Permissions-Policy / X-XSS-Protection 全部未下发
#   （而注释写在路径之前的区块——favicon / assets / *.html——均正常生效）。
#   因此本文件所有注释一律置于路径行之前。

# 安全响应头（全站）
# ⚠️ 2026-09-21 实测：本文件中「具体路径」的区块（favicon/assets/*.html）均正常生效，
# 但 `/*` 通配区块始终未下发（用一次性指纹头 X-Build-Stamp 验证确认，非缓存问题）。
# 因此以下安全头当前**在 pages.dev 上不生效**，需在 Cloudflare 控制台
# （Pages 项目 → Settings → Functions/Headers 或 Rules → Response Header Transform）
# 配置等价规则才能落地——属于需要登录控制台的操作。
/*
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()
  X-XSS-Protection: 0

# 强制 HTTPS（HSTS，1 年；Cloudflare Pages 已全站 HTTPS）
/*
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

# favicon 强缓存（内容不变，1 年）
/favicon.svg
  Cache-Control: public, max-age=31536000, immutable

# 静态资源强缓存（带 hash 的 assets，Vite 产物文件名含内容指纹）
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# HTML 不缓存，确保用户始终拿到最新版本
/*.html
  Cache-Control: no-cache

# API 响应不缓存（避免敏感数据被浏览器/代理缓存）
/api/*
  Cache-Control: no-store
