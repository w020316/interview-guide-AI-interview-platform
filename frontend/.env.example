# 前端环境变量示例
#
# 本地开发：
#   - 留空则走 Vite 代理（/api -> http://localhost:8080，见 vite.config.ts）
#   - 或填本地后端地址直连：VITE_API_BASE_URL=http://localhost:8080
#
# Vercel 部署：
#   - 留空则走 vercel.json rewrites 代理到 Render 后端（同源请求）
#
# Cloudflare Pages 部署（推荐，国内访问更好）：
#   - 必须填 Render 后端地址（Cloudflare Pages 不支持 rewrites 代理）
#   - 在 Cloudflare Pages 控制台 → Settings → Environment variables 中配置
VITE_API_BASE_URL=https://interview-guide-backend.onrender.com
